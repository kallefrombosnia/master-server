extends layout

block  user
    div.col-md-12
        h2.push-top User information:
        table.table.table-bordered
            thead.head-default
                tr
                    th(scope='col') Country
                    th(scope='col') City
                    th(scope='col') ZIP
                    th(scope='col') IP
                    th(scope='col') ISP
                    th(scope='col') Lat
                    th(scope='col') Lon
            tbody 
                tr
                    td #{info.country}
                    td #{info.city}
                    td #{info.zip}
                    td #{info.query}
                    td #{info.isp}
                    td #{info.lat}
                    td #{info.lon}
    div.col-md-12
        iframe(width='100%', height='250', frameborder='0', scrolling='no', marginheight='0', marginwidth='0', src=`https://maps.google.com/maps?q=${info.lat},${info.lon}&hl=en&z=14&output=embed`)
            br
            small
                a(href=`https://maps.google.com/maps?q=${info.lat},${info.lon}&hl=en;z=14&output=embed`, style='color:#0000FF;text-align:left', target='_blank')
                    | See map bigger
                